name: Greetings

on: pull_request

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hey, thank you very much for working on the project!'' first issue'
        pr-message: '!!!'' first pr'
